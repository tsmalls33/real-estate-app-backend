// Mirrors the PlanPeriod enum in prisma/schema.prisma.
// Once `prisma generate` is run you can import directly from '@prisma/client' instead.
export enum PlanPeriod {
  MONTHLY = 'MONTHLY',
  YEARLY = 'YEARLY',
}
